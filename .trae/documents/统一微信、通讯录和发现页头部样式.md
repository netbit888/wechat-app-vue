我需要将微信、通讯录和发现页的头部统一成微信原生样式，具体修改如下：

1. **修改发现页（Discover.vue）**：
   - 去掉左侧返回按钮
   - 在右侧添加搜索和添加按钮
   - 统一头部样式为微信标准

2. **统一微信聊天列表页（ChatList.vue）**：
   - 将Button组件替换为简单的图标按钮
   - 确保按钮样式与通讯录页一致

3. **统一头部样式**：
   - 确保三个页面的头部高度、背景色、边框样式一致
   - 确保标题的字体大小和颜色一致
   - 确保按钮的间距和大小一致

4. **修改头部结构**：
   - 所有页面头部采用相同的HTML结构
   - 标题居中显示
   - 右侧放置搜索和添加按钮

通过这些修改，将三个页面的头部统一成微信原生应用的设计风格，提升用户体验。