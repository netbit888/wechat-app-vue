# 完善个人资料页面

## 当前状态
- **文件**：`d:\mobile\wechat-app-vue\src\views\Me\Settings\Profile.vue`
- **当前内容**：仅显示"功能开发中…"的简单页面
- **目标**：实现微信风格的个人资料页面，包含头像、名字、性别等13个项目

## 功能需求
根据提供的图片，个人资料页面需要包含以下项目：
1. 头像
2. 名字
3. 性别
4. 地区
5. 手机号
6. 微信号
7. 我的二维码
8. 拍一拍
9. 签名
10. 来电铃声
11. 我的地址
12. 我的发票抬头
13. 微信豆

## 实现方案

### 1. 重写Profile.vue组件
**文件**：`src/views/Me/Settings/Profile.vue`

### 2. 组件结构
- **顶部导航栏**：包含返回按钮和页面标题
- **个人资料列表**：使用SettingsItem组件构建每个资料项
- **数据绑定**：从userStore获取用户信息
- **事件处理**：为每个项目添加点击事件

### 3. 实现细节

#### 模板结构
```html
<template>
  <div class="profile-page">
    <!-- 顶部导航栏 -->
    <header class="sub-header">
      <button class="back-btn" @click="$router.back()">⬅</button>
      <h2>个人资料</h2>
    </header>
    
    <!-- 个人资料列表 -->
    <main class="profile-content">
      <!-- 头像 -->
      <SettingsItem 
        title="头像" 
        description="" 
        @click="goToAvatarSetting"
      />
      
      <!-- 名字 -->
      <SettingsItem 
        title="名字" 
        :description="userInfo.nickname || '未设置'" 
        @click="goToNameSetting"
      />
      
      <!-- 其他项目... -->
    </main>
  </div>
</template>
```

#### 脚本部分
```javascript
import { computed } from 'vue'
import { useRouter } from 'vue-router'
import { useUserStore } from '@/composables/useStore'
import SettingsItem from '@/components/business/Me/SettingsItem.vue'

export default {
  name: 'Profile',
  components: { SettingsItem },
  setup() {
    const router = useRouter()
    const userStore = useUserStore()
    
    const userInfo = computed(() => userStore.currentUser || {})
    
    // 点击事件处理函数
    const goToAvatarSetting = () => {
      console.log('跳转到头像设置')
      // 实际实现中应该跳转到头像设置页面
    }
    
    // 其他事件处理函数...
    
    return {
      userInfo,
      goToAvatarSetting,
      // 其他函数...
    }
  }
}
```

#### 样式部分
- 保持与微信风格一致的样式
- 使用现有的CSS变量
- 确保列表项的样式统一

## 实现步骤
1. 修改`src/views/Me/Settings/Profile.vue`文件
2. 实现顶部导航栏
3. 使用SettingsItem组件构建13个资料项
4. 添加点击事件处理函数
5. 绑定用户数据
6. 调整样式以匹配微信风格
7. 测试页面功能

## 预期效果
- ✅ 实现微信风格的个人资料页面
- ✅ 包含所有13个资料项
- ✅ 显示用户当前信息
- ✅ 每个项目可点击（暂时跳转到通用页面或输出日志）
- ✅ 页面样式与微信一致
- ✅ 响应式设计，适配不同屏幕尺寸

## 功能说明
- 所有资料项均使用SettingsItem组件实现
- 点击每个项目会触发相应的处理函数
- 目前处理函数仅输出日志，后续可扩展为跳转到具体设置页面
- 页面标题为"个人资料"
- 包含返回按钮，点击返回上一页

## 技术要点
- 使用Vue 3 Composition API
- 从Vuex store获取用户数据
- 组件化开发，复用SettingsItem组件
- 遵循微信设计规范
- 代码结构清晰，易于维护和扩展